#include "../Carthage/Checkouts/xcconfigs/Configurations/Debug.xcconfig"
#include "../Carthage/Checkouts/xcconfigs/Targets/Tests.xcconfig"
#include "App-Common.xcconfig"

__PRODUCT_BUNDLE_IDENTIFIER_SUFFIX = .test

_ENVIRONMENTS = ENV_TESTS
_BUNDLE_NAME = DutieSplitTests
_BUNDLE_INFOPLIST_PATH = $(SRCROOT)/DutieSplitTests/Supporting Files/Info.plist

_CODESIGN_STYLE =
_CODESIGN_DEVELOPMENT_TEAM =
_CODESIGN_IDENTITY =
_CODESIGN_PROFILE_SPECIFIER =

TEST_HOST = $(BUILT_PRODUCTS_DIR)/DutieSplit.app/DutieSplit
BUNDLE_LOADER = $(TEST_HOST)
LD_RUNPATH_SEARCH_PATHS = $(inhertited) @executable_path/Frameworks @loader_path/Frameworks
COPY_PHASE_STRIP = NO
STRIP_INSTALLED_PRODUCT = NO
